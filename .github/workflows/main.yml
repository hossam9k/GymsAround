name: First Workflow

on: 
  push:
    branches: 
      -  main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print a welcome message
        run: echo "Weclome to our Girhub Actions Workflow!"
